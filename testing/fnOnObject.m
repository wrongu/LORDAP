function val = fnOnObject(obj)
% A placeholder function that takes in a MyObject instance. Note that loadOrRun will not work when
% given handles to function methods directly, like @obj.add

val = obj.add();

end

